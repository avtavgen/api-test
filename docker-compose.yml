services:
  web:
    build: src/
    command: fastapi run src/main.py --port 80
    environment:
      - DATABASE_URL=postgresql+asyncpg://postgres:postgres@db:5432/foo2
    volumes:
      - .:/app
    ports:
      - "80:80"
    depends_on:
      - db

  db:
    image: postgres:17.6-alpine
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=foo2
    volumes:
      - pg_data:/var/lib/postgresql/data

volumes:
  pg_data: